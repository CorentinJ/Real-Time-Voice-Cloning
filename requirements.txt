# python3.7 or less
tensorflow==1.15
umap-learn
visdom
webrtcvad-wheels
librosa>=0.5.1
matplotlib>=2.0.2
numpy>=1.14.0
scipy>=1.0.0
tqdm
sounddevice
Unidecode
inflect
PyQt5
multiprocess
numba==0.48.0
unidecode
PySoundFile

## Possible linux fix for webrtcvad failure
#sudo apt install python3 python3-dev build-essential libssl-dev libffi-dev libxml2-dev libxslt1-dev zlib1g-dev python-pip

## Possible linux fix for 'OSError: PortAudio library not found'
# sudo apt-get install libportaudio2

# Fix for "This application failed to start because no Qt platform plugin could be initialized. Reinstalling the application may fix this problem."
# sudo apt-get install libxkbcommon-x11-dev